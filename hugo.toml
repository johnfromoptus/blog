baseURL = "https://mundane.computer/"
languageCode = "en-us"
title = "Mundane Computer"
theme = "PaperMod"
[pagination]
  pagerSize = 10

enableRobotsTXT = true
buildDrafts = false
buildFuture = false
buildExpired = false

[params]
  env = "production"
  description = "Projects, notes, and things I'm learning."
  author = "Mundane"
  images = ["images/og-default.png"]
  defaultTheme = "dark"  # dark | light | auto
  ShowReadingTime = true
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  ShowCodeCopyButtons = true
  ShowToc = true
  TocOpen = false

  [params.homeInfoParams]
    Title = "hey"
    Content = """
I tinker with homelabs, self-hosted infrastructure, and whatever else catches my attention. \
This is where I write about what I'm building and learning.
"""


[params.profileMode]
  enabled = false  # flip to true if you want a profile landing page instead

# Search (PaperMod has built-in search)
[outputs]
  home = ["HTML", "RSS", "JSON"]

# Menu
[[menu.main]]
  identifier = "posts"
  name = "posts"
  url = "/posts/"
  weight = 10

[[menu.main]]
  identifier = "search"
  name = "search"
  url = "/search/"
  weight = 30

[[menu.main]]
  identifier = "x"
  name = "𝕏"
  url = "https://x.com/MundaneComputer"
  weight = 40

# Markup config
[markup]
  [markup.highlight]
    style = "dracula"
    lineNos = false
    codeFences = true
    guessSyntax = true
  [markup.goldmark.renderer]
    unsafe = true  # allows raw HTML in markdown if needed
