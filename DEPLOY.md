# Cloudflare Pages build configuration
# Set these in the Cloudflare Pages dashboard:
#
#   Build command:    hugo --minify
#   Build output:     public
#   Root directory:   /  (or wherever your hugo.toml lives)
#
# Environment variable:
#   HUGO_VERSION = 0.145.0  (or latest)
#
# That's it. Cloudflare Pages auto-deploys on push to main.
